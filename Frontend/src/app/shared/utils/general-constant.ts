import { Editor } from '@tiptap/core';
import { EditionOption } from '../../marks/interfaces/edition-option';

export class GeneralConstant {
    public static token_storage_key: string = 'markit-token';
}