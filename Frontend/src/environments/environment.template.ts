// Markit Frontend Environment Configuration Template
export const environment = {
  // Production mode flag
  // Values: 'True' or 'False' (as strings)
  // Default for development: 'False'
  production: '${production}', // 'True' or 'False'
  
  // Backend API base URL
  // Must match the backend API URL where your .NET API is running
  baseApiUrl: '${baseApiUrl}', // ex.'http://localhost:5000'
};