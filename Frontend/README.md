# MarkitApp

## Development server

1. Copy the file `environment.template.ts` and generate the file `environment.ts`, ensure that you fill the required information for the develop env
2. Install dependencies if necessary: (The project uses pnpm as package manager) `pnpm install`
3. Run `pnpm start` for a dev server. Navigate to `http://localhost:4200/`.